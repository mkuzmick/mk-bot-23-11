const { llog } = require('../../ll-modules/ll-utilities')
const fs = require('fs')

const liveStillCaptureBot = async () => {
    // look for new file in folder
    
    llog.magenta('liveStillCaptureRequest')

}

